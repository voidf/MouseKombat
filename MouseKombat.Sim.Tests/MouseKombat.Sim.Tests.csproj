<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <AssemblyName>MouseKombat.Sim.Tests</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\MouseKombat.Sim\MouseKombat.Sim.csproj" />
    <!-- Also the runner for the network layer: framing, room rules and (next) a loopback
         host/client handshake. One console runner keeps "one command runs everything" true. -->
    <ProjectReference Include="..\MouseKombat.Net\MouseKombat.Net.csproj" />
  </ItemGroup>
</Project>
